# Мой день — PWA приложение

## Запуск
- Локально: `npx serve public`
- Docker: `docker-compose up --build`

## Структура
- Core: роутер, UI-контейнер, сервис данных
- Модули: профиль, заметки, привычки, статистика
- PWA: офлайн-режим, установка на устройство